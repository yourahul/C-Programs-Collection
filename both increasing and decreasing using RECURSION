#include<stdio.h>
void increasing(int n){
	if(n==0) return; //base code
	printf("%d \n",n); 
	increasing(n-1); //recursive call
	printf("%d \n",n); //print	
	return;
}
int main(){
	int n;
	printf("Enter the number:");
	scanf("%d",&n);
	increasing(n);
	return 0;
}
