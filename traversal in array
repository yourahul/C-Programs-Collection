#include<stdio.h>

int main(){
	
	int arr[] = {13,56,78,23,89};
	int size = (sizeof(arr)/sizeof(arr[0])); //calculate the number of elements
	
	for(int i=0; i<5; i++)
	{
		
		printf("Element at index: %d %d \n",i,arr[i]);
	} 
	return 0;
}
